{
  "name": "@anymodel/nginx",
  "version": "0.2.1",
  "description": "NGINX models.",
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "watch": "tsc -b -w"
  },
  "dependencies": {
    "ts-essentials": "^9.3.0"
  },
  "publishConfig": {
    "directory": "lib"
  }
}
